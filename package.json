{
  "name": "admission-guidelines-automation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server/server.js",
    "dev": "node --watch-path=src --watch-path=data src/server/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@notionhq/client": "latest",
    "dotenv": "latest"
  },
  "engines": {
    "node": ">=20"
  }
}
