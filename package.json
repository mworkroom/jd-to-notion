{
  "name": "jd-to-notion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server/server.js",
    "dev": "node --watch-path=src --watch-path=data src/server/server.js",
    "test": "node --test",
    "test:browser": "playwright test"
  },
  "dependencies": {
    "@notionhq/client": "latest",
    "adm-zip": "^0.6.0",
    "dotenv": "latest",
    "googleapis": "^176.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
