# 2026-07-30 Admission Guidelines Automation Development Log

## Work Summary

기존 Word VBA 매크로에서 사용하던 입학요강 작업 폴더 자동 생성 기능을 앱의 `Word 파일 만들기` 동작에 통합했다. DOCX 저장 위치와 폴더명 규칙은 기존 매크로 흐름을 유지하면서, 앱의 비덮어쓰기 저장과 오류 처리 방식을 적용했다.

## Work Completed

- `C:\Users\Marion\Desktop\폴더자동생성.txt`의 VBA 매크로를 분석해 폴더명 규칙과 저장 순서를 확인했다.
- Word 생성 요청의 최종 학생명과 Programme Label로 `학생명_Programme Label` 폴더명을 만들도록 구현했다.
- 학생명 끝의 호칭 `님`은 폴더명에서 제거하고 Windows 폴더명 금지 문자는 제거하도록 했다.
- `WORD_OUTPUT_DIR`에 폴더가 없으면 생성하고, 이미 존재하는 정상 폴더는 그대로 재사용하도록 했다.
- 폴더 경로에 동명의 파일이 있으면 DOCX를 저장하지 않고 사용자용 오류로 중단하도록 했다.
- 새 폴더 생성 후 DOCX 저장이 실패하면 비어 있는 새 폴더를 정리하도록 했다.
- 생성 성공 응답과 화면에 Word 파일 경로, 작업 폴더 경로, 신규 생성 또는 기존 폴더 재사용 여부를 함께 표시하도록 했다.
- README와 Phase 5 계획의 저장 규칙·완료 기준을 현재 동작에 맞게 갱신했다.
- 폴더명 변환, 최초 생성, 기존 폴더 재사용, 동명 파일 충돌을 자동 테스트에 추가했다.

## Decisions

- 기존 매크로와 동일하게 DOCX는 Desktop에 직접 저장하고, 작업 폴더도 Desktop에 나란히 생성한다. DOCX를 작업 폴더 안으로 자동 이동하지 않는다.
- 기존 VBA의 덮어쓰기 질문은 앱의 안전한 번호 저장 규칙으로 대체한다. 같은 DOCX가 있으면 `(2)`, `(3)`을 붙이고 기존 파일을 보존한다.
- 폴더 생성은 DOCX 게시 전에 완료해 성공 응답이 파일과 폴더 준비를 모두 보장하도록 했다.
- 학생 이름에 포함될 수 있는 글자를 과도하게 제거하지 않기 위해 호칭은 문자열 전체가 아니라 끝의 `님`만 제거한다.

## Current Status

- 전체 자동 테스트 93개가 모두 통과했다.
- 실제 등록된 DOCX 템플릿과 임시 출력 경로로 샘플을 생성해 Word 파일과 `테스트학생_Real Estate` 폴더가 함께 만들어지는 것을 확인한 뒤 임시 결과를 정리했다.
- 실제 Desktop에는 검증용 파일이나 폴더를 만들지 않았다.
- 최신 로컬 서버로 재시작했으며 Word 상태와 Notion 상태가 모두 정상이다.

## Next Steps

- 실제 업무 요청에서 `Word 파일 만들기`를 눌러 Desktop에 DOCX와 작업 폴더가 함께 생성되는지 확인한다.
- 폴더 안에 이후 어떤 자료를 자동으로 분류할지 필요가 생기면 별도 규칙으로 확장한다.
- 첫 실제 DOCX는 Microsoft Word에서 템플릿 서식과 페이지 배치를 계속 시각적으로 검토한다.

## Work Summary - Programme Label 선택 규칙 수정

Nutrition 계열 5개 전공의 대표 전공명이 `Nutrition`이 아니라 `and Public Health`로 생성되는 문제를 조사하고 선택 규칙을 수정했다.

## Work Completed - Programme Label 선택 규칙 수정

- 공통 구절의 길이만 우선하던 계산을 여러 전공에 등장하는 횟수 우선으로 변경했다.
- `Nutrition`처럼 의미 있는 한 단어 전공명도 대표 전공명 후보로 허용했다.
- `and`, `with`, `of` 같은 연결어로 시작하거나 끝나는 부자연스러운 후보를 제외했다.
- 실제 박선영님 요청의 5개 Nutrition 전공을 회귀 테스트로 추가해 결과가 `Nutrition`인지 검증했다.
- 기존 `Biomedical Sciences with` 결과를 자연스러운 `Biomedical Sciences`로 수정하고 회귀 테스트를 갱신했다.

## Decisions - Programme Label 선택 규칙 수정

- 대표 전공명은 일부 전공에만 나타나는 긴 구절보다 더 많은 전공을 설명하는 핵심어를 우선한다.
- 등장 횟수가 같을 때는 더 긴 의미 있는 구절을 선택한다.
- 연결어로 시작하거나 끝나는 구절은 파일명과 머리말에 적합하지 않으므로 후보에서 제외한다.

## Current Status - Programme Label 선택 규칙 수정

- 박선영님 사례가 `Nutrition`으로 생성되는 것을 자동 테스트로 확인했다.
- `test/rules.test.js` 집중 테스트 13개가 모두 통과했다.
- 전체 자동 테스트 94개가 모두 통과했다.

## Next Steps - Programme Label 선택 규칙 수정

- 실제 화면에서 동일 요청을 다시 추출해 Programme Label과 Word 파일명이 `Nutrition`으로 표시되는지 확인한다.
- 다양한 전공 조합에서 지나치게 일반적인 한 단어가 선택되는 사례가 발견되면 의미어 필터를 별도 보강한다.
