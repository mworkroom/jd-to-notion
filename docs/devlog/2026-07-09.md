# 2026-07-09 Admission Guidelines Automation Development Log

## Work Summary

Phase 2 사양을 기준으로 기존 구현을 점검했고, Notion 스키마 검증에서 실제 문서화된 관계 속성명을 그대로 사용하지 않는 부분을 수정했다. 이미 Phase 2 구조와 테스트 대부분은 구현되어 있었으므로, 누락된 스키마 이름 정합성과 사용자 문서의 실행 경로를 보정하는 데 집중했다.

## Work Completed

* Students 데이터 소스의 Agents 관계 속성명을 `Agent DB`로 변경했다.
* Majors 데이터 소스의 Universities 관계 속성명을 `Universities DB`로 변경했다.
* 스키마 상수가 문서화된 관계 속성명을 유지하는지 확인하는 테스트를 추가했다.
* 화면 상단의 단계 표시를 Phase 2로 갱신했다.
* README의 Windows 실행 경로를 현재 프로젝트 경로로 수정하고, Notion 관계 속성명 확인 절차를 추가했다.
* `npm test`를 실행해 전체 44개 테스트가 통과하는 것을 확인했다.

## Decisions

Notion 관계 속성명은 유사한 이름으로 추정하지 않고 사양에 적힌 정확한 이름을 사용하도록 결정했다. Phase 2는 읽기 전용 검증 단계이므로, 실제 스키마가 다를 경우 앱이 임의로 맞춰 읽기보다 스키마 불일치로 명확히 보고하는 편이 안전하다.


## Current Status

Phase 2의 Notion 스키마 검증과 repository 읽기 로직은 문서화된 `Agent DB`, `Universities DB` 관계 속성명을 기준으로 동작한다. 전체 자동 테스트는 통과했으며, Phase 1 추출 기능도 Notion 미설정 상태에서 계속 동작하는 것으로 확인됐다.

## Next Steps

* 실제 Notion 워크스페이스에서 `Check connection`을 실행해 다섯 개 데이터 소스의 접근 권한과 속성명을 확인해야 한다.
* 라이브 데이터로 신규 학생, 기존 학생, 동명이인, 누락된 Major, 누락된 University 시나리오를 수동 검증해야 한다.
* Phase 3에서 쓰기 작업을 추가하기 전까지 `Create Notion records - Phase 3` 버튼은 계속 비활성화 상태로 유지한다.

---

## Work Summary

실제 테스트 중 학생 이름에 `님`이 붙어서 추출되고, 학과명과 링크를 함께 붙여넣은 경우 프로그램이 0개로 인식되는 문제가 확인되어 Phase 1 추출 파서를 보완했다. 기존 자동 테스트는 통과했지만 실제 입력 형태 일부가 테스트에 포함되지 않았기 때문에 회귀 테스트를 추가했다.

## Work Completed

* `[업무요청] 존칭학생님 입학요강`처럼 이름과 `님`이 붙은 경우 Student base name을 `존칭학생`으로 추출하도록 수정했다.
* `MSc Computer Science https://...`처럼 학과명과 URL이 같은 줄에 있는 입력을 프로그램으로 인식하도록 수정했다.
* `[MSc Biomedical Sciences](https://...)`처럼 학과명이 마크다운 링크 텍스트인 입력도 프로그램으로 인식하도록 수정했다.
* 위 입력 형태를 검증하는 서버 테스트 2개를 추가했다.
* 전체 테스트 46개 통과를 확인했다.
* 포트 3000에서 이전 코드로 떠 있던 로컬 서버를 종료하고 수정된 서버를 다시 실행했다.

## Decisions

학과명과 링크가 별도 줄에 있을 때만 프로그램을 만드는 방식은 실제 복붙 흐름에 취약했다. 사용자가 Notion이나 웹페이지에서 복사할 때 한 줄 URL, 마크다운 링크, 일반 텍스트 링크가 섞일 수 있으므로 URL 라인에서도 학과명을 추출하도록 했다.

학생 이름의 `님` 제거는 한글에서 `\b` 단어 경계가 안정적으로 동작하지 않아 `님` 뒤가 공백 또는 문자열 끝인 경우를 직접 확인하는 정규식으로 처리했다.

## Current Status

추출 API는 `존칭학생님`을 `존칭학생`으로 반환하고, 학과명과 URL이 같은 줄인 프로그램 2개를 정상 인식하는 것을 로컬 서버에서 확인했다. `npm test` 기준 전체 46개 테스트가 통과한다.

## Next Steps

* JANDI 실제 메시지 샘플을 더 모아 줄바꿈, 마크다운, 불릿 기호 변형을 추가 테스트로 보강한다.
* 현재 추출기는 여전히 mocked extraction이므로, Phase 3 이후 실제 AI 추출을 붙일 때도 이 로컬 규칙을 안전장치로 유지할지 결정한다.

---

## Work Summary

GitHub 공개 또는 비공개 저장소에 올릴 수 있도록 `.gitignore`를 다시 점검했다. 실제 Notion 토큰과 로컬 산출물은 제외하되, 앱 실행에 필요한 샘플 환경 파일과 대학 별칭 CSV는 포함되도록 정리했다.

## Work Completed

* `.pnpm-store/`, 로컬 에이전트 설정 폴더, 백업 압축파일, 임시파일, 생성 문서 출력물을 ignore 목록에 추가했다.
* 전체 `*.csv`는 기본적으로 제외하되, 앱 동작에 필요한 `data/universityAliases.csv`는 예외로 포함되게 했다.
* `rg --files` 기준으로 `.env`는 제외되고 `.env.example`, `data/universityAliases.csv`, 소스와 테스트 파일은 포함 후보로 보이는 것을 확인했다.

## Decisions

실제 업무 메시지, 생성된 Word/PDF, 로컬 백업 압축파일은 개인정보가 섞일 가능성이 높으므로 기본적으로 Git에 올리지 않기로 했다. 반면 `data/universityAliases.csv`는 공개 대학 도메인과 별칭 매핑이며 앱 실행에 필요하므로 저장소에 포함하는 쪽으로 결정했다.

## Current Status

현재 `.gitignore`는 GitHub 저장소 생성 전에 사용할 기본 안전장치로 정리되어 있다. 아직 repo 초기화, commit, push는 진행하지 않았다.

## Next Steps

* GitHub repo를 만들기 전에 `git status --ignored` 또는 새 repo에서 `git check-ignore`로 최종 포함/제외 파일을 확인한다.
* 커밋 전 `.env`, 실제 JANDI 원문, 학생별 산출물, 로컬 백업 zip이 staging에 들어가지 않았는지 확인한다.

---

## Work Summary

실제 JANDI 메시지에서 `Goldsmiths - MA Contemporary Art Theory`처럼 대학명과 학과명이 한 줄에 하이픈으로 연결된 형식을 프로그램으로 인식하지 못하는 문제가 확인되어 추출 파서를 복구했다.

## Work Completed

* `대학명 - 학과명` 형식의 줄을 대학명과 프로그램명으로 분리하도록 추출 로직을 추가했다.
* `Essex -MA Art History and Theory`처럼 하이픈 뒤 공백이 없는 변형도 처리하도록 했다.
* 왼쪽 조각이 실제 대학 별칭 또는 대학명으로 확인되는 경우에만 이 규칙을 적용해, 학과명 내부 하이픈과 충돌하지 않게 했다.
* 실제 형식의 테스트 메시지를 기반으로 프로그램 4개 인식 테스트를 추가했다.
* 전체 테스트 47개 통과를 확인했다.
* 로컬 서버를 새 코드로 재시작하고 `/api/extract`에서 가상 학생명, 프로그램 4개, 에러 없음 응답을 확인했다.

## Decisions

실제 업무 메시지에서는 대학명과 학과명이 별도 줄로 정리되지 않고 `대학명 - 학과명` 형식으로 붙는 경우가 있으므로, 이 형식을 Phase 1 추출기의 기본 지원 범위에 포함하기로 했다. 다만 학과명에도 하이픈이 포함될 수 있어, 왼쪽 텍스트가 대학으로 판정될 때만 분리하도록 제한했다.

## Current Status

J님이 제공한 실제 형식의 샘플은 Analyze 단계에서 프로그램 4개를 자동 인식한다. 현재 로컬 서버는 수정된 코드로 실행 중이다.

## Next Steps

* 추가 실제 JANDI 샘플이 있으면 같은 방식으로 회귀 테스트를 보강한다.
* Phase 3 작업 전까지 현재 추출 가능 범위를 기준선으로 삼고, 새 변경 전후에 `/api/extract` 샘플 테스트를 먼저 확인한다.
